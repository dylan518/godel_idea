# `paper_darwin_bundle`

Auto-generated by `python3 ideas/build_darwin_paper_bundle.py`.

| File | Purpose |
|------|---------|
| `REPORT.md` | Human-readable trajectory, compare index, mechanisms, baselines note, cost hints, paper outline |
| `METHODS.md` | **Draft methods** (Darwin loop, benchmark, judges, selection, multi-judge harness); includes **M1–M6 agent-call split** |
| `trajectory.json` | Raw `evolution_log.jsonl` + git SHA + `CURRENT_VERSION` |
| `compares_index.json` | Parsed top-level `results/compare_*.json` metadata |
| `mechanisms_from_docstrings.json` | Docstring previews: `S_paper` (reboot baseline, `S_sota.py`), `S12`, `S15`, `S_paper_skills` (`S_paper.py`) |
| `rejection_reason_samples.json` | Example judge reasons where champion beat a sub-threshold candidate |
| `rejection_keyword_buckets.json` | Naive keyword counts over those samples |
| `publish_eval_summaries.json` | Any `publish_eval_*/summary.json` found |

**Replication #2 (running separately):**  
`ideas/results/publish_eval_rep2_small/` — 5-topic `publish_multi_judge` run; log at `ideas/results/_rep2_small_publish.log`.

Regenerate after new compares or log edits:

```bash
python3 ideas/build_darwin_paper_bundle.py
```
