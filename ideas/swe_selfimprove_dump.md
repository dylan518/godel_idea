# SWE Self-Improvement Proposal — S15

Generated by `debug_generate_proposal.py`.
Shows exactly what the generate_idea() meta-pipeline receives as its topic,
and the full structured proposal it returns for Claude Code to implement.

======================================================================

## INPUT: self-improvement topic fed to generate_idea()

======================================================================
**Size: 3,152 chars (788 tokens est.)**

```
Self-improvement of a research idea generation pipeline (S15).

CURRENT PIPELINE OVERVIEW:
S12_r3: Hypothesis-First Adversarial Loop.

Replaces the top-down tree/tournament with a FALSIFIABLE-HYPOTHESIS-FIRST loop:

1. HYPOTHESIS GENERATION: One LLM call produces 5 sharp, testable scientific
   hypotheses about the topic (e.g., "scaling laws break under data-constrained
   regimes because X"). Starting from *claimed truths about the world* rather
   than technique names.

2. ADVERSARIAL

RECENT FAILURES (ideas where the current system lost to a weaker baseline):
### Topic: Mechanistic interpretability of transformer models
**Champion (S15) — 33/40 — WON:**
IDEA:  
We propose to rigorously test whether transformer attention heads responsible for syntactic parsing encode discrete, fully extractable symbolic grammar rules that can reconstruct parse trees with near-lossless fidelity compared to the original model’s predictions.

BACKGROUND:  
While transformer language models exhibit strong syntactic parsing capabilities, it remains an open question whether this knowledge is stored as discrete, algorithmic rules or distributed, continuous representations. Prior work on structural probes and mechanistic interpretability has revealed correlations betw

**Candidate (S16) — 26/40 — LOST:**
IDEA:  
Mechanistic interpretability of transformer models can be advanced by the **Hybrid Factorized-Nonlinear Encoding Mechanism**, which posits that algorithmic subroutines are encoded in sparse, low-dimensional embeddings derived from weight factorization combined with nonlinear, layer-wise interaction modeling to capture cross-layer dynamics, enabling improved prediction and manipulation of activation patterns.

BACKGROUND:  
Understanding how transformer models internally represent algorithmic subroutines remains an open challenge in mechanistic interpretability, with prior works often l

**Judge:** Idea B presents a more focused, testable, and ambitious hypothesis with clearer experimental validation, directly tackling a fundamental open question. W

PREVIOUSLY ATTEMPTED FIXES THAT DID NOT WORK:
(none yet — first round)

Your task: propose a FUNDAMENTALLY DIFFERENT ideation strategy for the APPROACH section.
Prompt-level tweaks (e.g. 'add a sentence to the critique prompt') are NOT acceptable.
Think about the ideation algorithm itself — how ideas are generated, selected, and refined.

Examples of the level of change we want:
- Replace hypothesis generation with contradiction-mining: find two conflicting recent papers and generate an idea that resolves the contradiction
- Add cross-domain transfer: map the topic to an analogous problem in a different field, adapt the best solution from that field
- Replace adversarial attack with a null-result adversary: generate the most boring/obvious outcome first, then force the idea to contradict it
- Add a 'what would falsify every prior approach' step before hypothesis generation
- Replace selection with a bet-hedging step: keep two competing hypotheses and build experiments that discriminate between them

Describe a concrete new algorithm or step in the APPROACH section.
```

======================================================================

## OUTPUT: raw generate_idea() response

======================================================================
**Size: 2,356 chars**

```
IDEA: We hypothesize that the minimal causal circuits identified by current interpretability tools (e.g., ACDC) for syntactic tasks do not contain discrete, extractable symbolic rules, but are instead transient, context-dependent activation patterns that serve as **causal but non-decomposable triggers** for distributed computations; this will be falsified if we can construct a synthetic transformer with known, implanted symbolic rules and show that our most advanced extraction techniques can recover them with near-perfect fidelity from its identified circuits.

BACKGROUND: Mechanistic interpretability assumes that model functions are localized in sparse, causal circuits. A robust anomaly challenges this: for a task like subject-verb agreement, ACDC identifies a minimal circuit of heads, yet ablating this circuit often causes only a shallow, context-sensitive performance drop, not a complete loss of the "rule." This suggests the circuit is necessary but not sufficient—the representation may be fundamentally non-symbolic and distributed.

APPROACH: We will develop a **causal fidelity probe** that tests for discrete rule encoding by intervening on a candidate circuit. Instead of extracting a rule, the probe will attempt to *steer* the circuit's activations to implement a counterfactual, candidate symbolic rule (e.g., flipping grammatical number) and measure if the model's output distribution changes **only** in the rule-governed way, without affecting unrelated syntactic properties.

EXPERIMENT: Using GPT-2 Small and the BLiMP minimal pairs dataset, we will: 1) Use ACDC to find the minimal circuit for a syntactic rule, 2) Train the causal fidelity probe to generate activations for a counterfactual rule, 3) Measure causal effect via **targeted syntactic intervention score** (TSIS: % change on the targeted rule's minimal pairs) vs. **collateral damage score** (CDS: % change on other, unrelated linguistic minimal pairs). We will run a critical **oracle test** on a synthetic transformer with known, hand-coded symbolic rules in specific attention heads.

NOVELTY: This directly tests the *decomposability* of circuit function—a core, implicit assumption of mechanistic interpretability—by demanding causal specificity, not just correlation. The synthetic model oracle definitively separates the hypothesis from tool limitations.
```

======================================================================

## OUTPUT: parsed fields

======================================================================

### IDEA

We hypothesize that the minimal causal circuits identified by current interpretability tools (e.g., ACDC) for syntactic tasks do not contain discrete, extractable symbolic rules, but are instead transient, context-dependent activation patterns that serve as **causal but non-decomposable triggers** for distributed computations; this will be falsified if we can construct a synthetic transformer with known, implanted symbolic rules and show that our most advanced extraction techniques can recover them with near-perfect fidelity from its identified circuits.

### BACKGROUND

Mechanistic interpretability assumes that model functions are localized in sparse, causal circuits. A robust anomaly challenges this: for a task like subject-verb agreement, ACDC identifies a minimal circuit of heads, yet ablating this circuit often causes only a shallow, context-sensitive performance drop, not a complete loss of the "rule." This suggests the circuit is necessary but not sufficient—the representation may be fundamentally non-symbolic and distributed.

### APPROACH

We will develop a **causal fidelity probe** that tests for discrete rule encoding by intervening on a candidate circuit. Instead of extracting a rule, the probe will attempt to *steer* the circuit's activations to implement a counterfactual, candidate symbolic rule (e.g., flipping grammatical number) and measure if the model's output distribution changes **only** in the rule-governed way, without affecting unrelated syntactic properties.

### EXPERIMENT

Using GPT-2 Small and the BLiMP minimal pairs dataset, we will: 1) Use ACDC to find the minimal circuit for a syntactic rule, 2) Train the causal fidelity probe to generate activations for a counterfactual rule, 3) Measure causal effect via **targeted syntactic intervention score** (TSIS: % change on the targeted rule's minimal pairs) vs. **collateral damage score** (CDS: % change on other, unrelated linguistic minimal pairs). We will run a critical **oracle test** on a synthetic transformer with known, hand-coded symbolic rules in specific attention heads.

### NOVELTY

This directly tests the *decomposability* of circuit function—a core, implicit assumption of mechanistic interpretability—by demanding causal specificity, not just correlation. The synthetic model oracle definitively separates the hypothesis from tool limitations.

======================================================================

## FINAL: task_description passed to Claude Code

======================================================================
**Size: 1,547 chars**
*(IDEA and NOVELTY fields dropped — only BACKGROUND→DIAGNOSIS, APPROACH→PROPOSED FIX, EXPERIMENT→EXPECTED IMPACT)*

```
DIAGNOSIS: Mechanistic interpretability assumes that model functions are localized in sparse, causal circuits. A robust anomaly challenges this: for a task like subject-verb agreement, ACDC identifies a minimal circuit of heads, yet ablating this circuit often causes only a shallow, context-sensitive performance drop, not a complete loss of the "rule." This suggests the circuit is necessary but not sufficient—the representation may be fundamentally non-symbolic and distributed.

PROPOSED FIX: We will develop a **causal fidelity probe** that tests for discrete rule encoding by intervening on a candidate circuit. Instead of extracting a rule, the probe will attempt to *steer* the circuit's activations to implement a counterfactual, candidate symbolic rule (e.g., flipping grammatical number) and measure if the model's output distribution changes **only** in the rule-governed way, without affecting unrelated syntactic properties.

EXPECTED IMPACT: Using GPT-2 Small and the BLiMP minimal pairs dataset, we will: 1) Use ACDC to find the minimal circuit for a syntactic rule, 2) Train the causal fidelity probe to generate activations for a counterfactual rule, 3) Measure causal effect via **targeted syntactic intervention score** (TSIS: % change on the targeted rule's minimal pairs) vs. **collateral damage score** (CDS: % change on other, unrelated linguistic minimal pairs). We will run a critical **oracle test** on a synthetic transformer with known, hand-coded symbolic rules in specific attention heads.

Edit history:
(none yet)
```

